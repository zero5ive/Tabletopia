{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "scripts": {
    "start:user": "cd user && npm run dev",
    "start:admin": "cd admin && npm run dev",
    "start:all": "concurrently \"npm run start:user\" \"npm run start:admin\""
  },
  "dependencies": {
    "@stomp/stompjs": "^7.2.0",
    "axios": "^1.12.2",
    "jwt-decode": "^4.0.0",
    "react-router-dom": "^7.9.1"
  }
}
